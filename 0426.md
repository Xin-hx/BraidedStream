# 0426 Multiscale Baseline Optimization Report

## 1. 任务与假设

**主任务（本次聚焦）**  
在 `Optimizing with Uncertainty` 分支中，将原先“局部 uncertainty baseline 上移”改为“基于不确定性变异的全局 multiscale 分摊”，并评估：

1. 平均斜率变化（Mean slope in ROI）  
2. 语义一致性变化（TPID Center Alignment + Uncertainty-Shift Coherence）

**核心假设**  
不确定性变异 `U_var(t)` 存在明显多尺度结构，因此局部 baseline 偏移 `Δ_local(t)` 可以通过多尺度显著性进行全局分配，而不是单尺度局部偏移。

---

## 2. 多尺度验证结果（Synthetic + Covid）

验证方法：对不确定性变异序列做 Haar dyadic 分解，观察尺度能量比例 `ρ_s` 是否分布于多个尺度（阈值 `0.08`，有效尺度数 >= 3 判定为通过）。

### Synthetic
- `effectiveScaleCount = 5`，`verified = true`
- 主要尺度能量：`128: 29.5%`，`32: 23.9%`，`16: 14.0%`
- 结论：能量非单峰，具备多尺度结构。

### Covid
- `effectiveScaleCount = 5`，`verified = true`
- 主要尺度能量：`8: 33.6%`，`16: 24.2%`，`2: 10.0%`
- 结论：短中尺度共同主导，明显多尺度。

---

## 3. 数学形式化

设：
- `g_ref(t)`：固定 `sineStream` 参考基线  
- `g_local(t)`：局部 uncertainty-aware 基线  
- `Δ_local(t) = g_local(t) - g_ref(t)`

### 3.1 不确定性变异

先聚合不确定性：

\[
\bar U(t)=\frac{1}{K}\sum_{k=1}^K U_k(t)
\]

再定义变异：

\[
U_{var}(t)=|\bar U(t)-\bar U(t-1)|
\]

### 3.2 Haar 多尺度分解

对 `U_var` 做 dyadic Haar 分解，得到尺度 `s` 的 detail 系数及能量：

\[
\rho_s=\sum_j d_{s,j}^2,\qquad
\lambda_s=\frac{\rho_s}{\sum_r \rho_r}
\]

并得到时刻显著性 `a_s(t)`（由该尺度 detail 在时间上的块状回投影的幅值）。

### 3.3 局部偏移的多尺度分摊

时刻内尺度归一化权重：

\[
\beta_s(t)=\frac{a_s(t)}{\sum_r a_r(t)+\varepsilon}
\]

多尺度重分配：

\[
\Delta_{ms}(t)=\sum_s \lambda_s\cdot \text{Smooth}_{2^s}\big(\beta_s(t)\cdot \Delta_{local}(t)\big)
\]

### 3.4 守恒与再约束

1. 去均值（避免整体漂移）  
2. L1 shift budget 对齐（保持总偏移量可比）：

\[
\sum_t |\Delta_{ms}(t)| \approx \sum_t |\Delta_{local}(t)|
\]

最终基线：

\[
g_{ms}(t)=g_{ref}(t)+\Delta_{ms}(t)
\]

---

## 4. 实现说明（局部 -> 全局分摊）

### 4.1 核心算法
- 文件：`src/core/baseline.ts`
- 新增：
  - `computeMultiscaleDistributedBaseline(...)`
  - `MultiscaleBaselineDiagnostics` 等诊断结构
- 回退策略：
  - 若 uncertainty 变异退化（空/平坦），回退到 `g_local`，并记录 `fallbackReason`。

### 4.2 场景接入（仅 Uncertainty 分支）
- 文件：`src/app/sceneBuilder.ts`
- `buildOptimizeUncertaintyComparisonScene(...)` 改为：
  - `before = local uncertainty baseline`
  - `after = multiscale distributed baseline`
- layer ordering 与 ROI 流程保持不变。

### 4.3 指标扩展与可视化
- 文件：`src/layout/metrics.ts`
  - 保留 `Mean slope in ROI`
  - 新增：
    - `TPID Center Alignment`（Spearman）
    - `Uncertainty-Shift Coherence`（Pearson）
  - 支持挂载 `multiscale diagnostics`
- 文件：`src/views/MetricsView.vue`, `src/styles.css`
  - 新增 multiscale energy bars（scale ratio 条形图 + verified/effective scales/fallback 展示）

---

## 5. 定量评估

评估口径：`before(local uncertainty) -> after(multiscale distributed)`。

| Dataset | Mean slope (before -> after) | Delta | TPID Center Alignment (before -> after) | Delta | Uncertainty-Shift Coherence (before -> after) | Delta |
|---|---:|---:|---:|---:|---:|---:|
| Synthetic | 0.1081 -> 0.1087 | +0.0006 | 0.5952 -> 0.6190 | +0.0238 | -0.8698 -> -0.4162 | +0.4536 |
| Covid | 15373.7062 -> 11603.7432 | -3769.9630 | -0.6135 -> -0.4707 | +0.1428 | -0.4734 -> 0.6461 | +1.1196 |

补充（multiscale 诊断）：
- Synthetic：verified=`yes`，effective scales=`5`
- Covid：verified=`yes`，effective scales=`5`

---

## 6. 可视化解读与结论

1. **假设成立**：Synthetic 与 Covid 均表现为多尺度不确定性变异（非单尺度主导）。  
2. **主目标达成（尤其在真实数据）**：Covid 上平均斜率显著下降，同时语义指标双提升。  
3. **语义改进稳定**：两数据集均提升 TPID 对齐和 Shift-Coherence，说明分摊后偏移更符合不确定性语义组织。  
4. **副作用可控**：Synthetic 上平均斜率有轻微上升，但语义一致性提升明显，属于可解释取舍。  

结论：本次 `Multiscale Baseline Optimization` 已实现“验证 -> 形式化 -> 算法 -> 可视化 -> 定量评估”闭环，可作为论文中 `Multiscale Baseline Optimization` 模块的实现与实证基础版本。
