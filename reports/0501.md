# 0501 PID new contour plot 检查记录

## 结论

当前 PID new contour plot 里出现许多不连续小圆圈，主要不是新的 PID 语义，也不是多个 depth rank 被同时画出，而是 `d3.contours()` 对阈值化 fuzzy mask 的忠实几何输出：同一个 mask 在 `membership >= 0.5` 后可能包含多个连通分量和内部孔洞，D3 会把这些分量与孔洞边界全部画成闭合环。

这些圆圈的含义是：在对应的 `(time, log1p(value))` 局部区域内，当前要画的 contour boxplot mask 在阈值 0.5 附近形成了独立的高 membership 区域，或者在一个大区域内部形成了低于阈值的空洞。对本项目的 time-value streamgraph 适配来说，它们通常表示离散时间采样、log 值域栅格化、fuzzy band 阈值化、union/intersection 聚合共同产生的小连通区域或孔洞；它们不应被读作单独的 ensemble member。

已修改 `src/render/pidNewChart.ts`：PID depth、排序、mean mask、union/intersection 的计算不变，只在渲染 `d3.contours()` 的结果后保留主外包络，并丢弃小连通分量与内部孔洞边界，使 PID new contour boxplot 更接近论文中 percentile envelope 的视觉语义。

## 论文方法核对

论文《Probabilistic Inclusion Depth for Fuzzy Contour Ensemble Visualization》将 contour 表示为 fuzzy/probabilistic mask：

```text
u: Omega -> [0, 1]
```

概率包含关系使用一个方向性 inclusion：

```text
u subset_p v = integral(u * v) / integral(u)
```

PID 对每个 contour 同时看两个方向：

```text
IN_in(ci)  = average_j (ui subset_p uj)
IN_out(ci) = average_j (uj subset_p ui)
PID(ci)    = min(IN_in(ci), IN_out(ci))
```

PID-mean 将完整的两两比较近似为与 ensemble mean mask 比较：

```text
meanMask(x) = average_i ui(x)
IN_mean_in(ci)  = ui subset_p meanMask
IN_mean_out(ci) = meanMask subset_p ui
PID_mean(ci)    = min(IN_mean_in(ci), IN_mean_out(ci))
```

论文中 contour boxplot 的可视化方法是：先用 PID/PID-mean 排序，再用指定百分位 population 的 surface union/intersection 形成 envelope；对 probabilistic maps，论文也说明在 PID-mean 后为了 surface visualization 需要二值化。也就是说，深度计算尽量保留 fuzzy 信息，但最终 surface/contour boxplot 是阈值后的 envelope 表达。

## 当前实现

`src/core/contourPid.ts` 中的 PID new 是论文 PID-mean 的 time-value 适配：

1. 每个 layer 的不确定性带被栅格化到 `(time, log1p(value))`。
2. `p25-p75` 或可替代的 inner band 作为 membership 1 的核心区域。
3. `p025-p975` 或可替代的 outer band 作为 fuzzy shoulder，membership 从 0 线性过渡到 1。
4. 所有 layer mask 求均值得到 `meanMask`。
5. 每个 layer 用 `dot(mask, meanMask) / area(mask)` 与 `dot(mask, meanMask) / area(meanMask)` 得到 in/out score，depth 取二者最小值。
6. boxplot mask 使用逐点聚合：all union 和 central union 用 `max`，central intersection 用 `min`，deepest member 取 depth 最大的单个 mask。

`src/render/pidNewChart.ts` 负责绘图。原实现直接用：

```text
d3.contours().thresholds([0.5])
```

从 mask 中提取所有等值线。因此只要 mask 在阈值后有多个连通区域或孔洞，就会出现多个闭合子路径，看起来像许多不连续的圆圈。

## 为什么会出现许多小圆圈

关键原因有四个：

1. `d3.contours()` 返回的是完整 `ContourMultiPolygon`，包含所有 disconnected components 和 holes。
2. time-value mask 是按离散时间列 rasterize 的；相邻时间的高 membership 区间如果在 y 方向重叠很少，阈值区域就可能被切成多个岛。
3. `log1p(value)` 会压缩大值、拉开低值附近的相对差异，某些局部波动在栅格中更容易形成小闭合区域。
4. union/intersection 是对 fuzzy mask 先做 `max/min` 再取 0.5 contour；这种 fuzzy-set 语义合理，但会保留局部孔洞与小岛。

所以，这些圆圈最严格的解释是“阈值化后的 mask 连通结构”。在分析上，它们可提示某些时间和值域附近的覆盖是断续的；在当前 overview 图上，它们更多造成视觉噪声，因为 PID new panel 的目标是显示主要 percentile envelope。

## 代码修改

修改文件：

```text
src/render/pidNewChart.ts
```

新增渲染过滤：

```text
contourFeatures(...)
  -> d3.contours()
  -> dominantEnvelopeFeature(...)
```

`dominantEnvelopeFeature` 对每个 contour feature：

1. 计算每个 polygon exterior ring 的面积。
2. 只保留面积最大的 exterior ring。
3. 丢弃 interior rings，也就是孔洞边界。

这会让四类 contour boxplot layer 都呈现为主外包络：

```text
all union
central 50% union
central 50% intersection
deepest member
```

注意：这是渲染层修正，不改变 PID-Mean depth、depth order、display order 或任何 mask 数值。若后续要做局部异常诊断，可以再加一个 debug/all-components 开关，把所有 D3 子分量重新显示出来。

## 验证

已运行：

```text
npm run build
```

结果：`tsc --noEmit` 与 `vite build` 均通过。
