**1. 当前 PID Layer Ordering 的实现（通俗版）**

核心代码在 [src/core/pidOrdering.ts](e:/Project/2026Journal/Code/src/core/pidOrdering.ts#L48)，当前视图调用在 [src/views/PidOrderingView.vue](e:/Project/2026Journal/Code/src/views/PidOrderingView.vue#L109)。

可以理解为一句话：  
`谁的“中位预测轨迹”更经常落在“其他层的不确定区间里”，谁就更“中心”，排序更靠前。`

具体步骤是：

1. 先给每个 layer 在每个时刻构造一个不确定区间 `[low, high]`。  
区间优先级是 `p025/p975` > `p05/p95` > `p10/p90` > `p25/p75` > `lower/upper` > `mean±unc`，见 [src/core/pidOrdering.ts](e:/Project/2026Journal/Code/src/core/pidOrdering.ts#L41) 和 [src/core/pidOrdering.ts](e:/Project/2026Journal/Code/src/core/pidOrdering.ts#L171)。

2. 每个 layer 取中心值 `center(t)`（默认 p50），并夹紧到区间内，见 [src/core/pidOrdering.ts](e:/Project/2026Journal/Code/src/core/pidOrdering.ts#L165)。

3. 对于 layer `i` 在时刻 `t`，看它的 `center_i(t)` 被多少其他 layer 的区间覆盖。  
这里有三个关键修正：
- 不算自己（`excludeSelf=true`）  
- 宽区间降权（避免“区间越宽越占便宜”）  
- 比较对象太少的时刻直接跳过（`minComparators=2`）  
见 [src/core/pidOrdering.ts](e:/Project/2026Journal/Code/src/core/pidOrdering.ts#L58) 到 [src/core/pidOrdering.ts](e:/Project/2026Journal/Code/src/core/pidOrdering.ts#L123)。

4. 对有效时刻求平均，得到 `PID depth`，再按 depth 降序排序。并用 `validTimeCount`、`totalMean` 作为并列打破规则，见 [src/core/pidOrdering.ts](e:/Project/2026Journal/Code/src/core/pidOrdering.ts#L125) 到 [src/core/pidOrdering.ts](e:/Project/2026Journal/Code/src/core/pidOrdering.ts#L144)。

---

**2. 和 Sine layer ordering 对比：发生了什么改动、对指标意味着什么**

Sine 排序（[src/core/optimizeOrder.ts](e:/Project/2026Journal/Code/src/core/optimizeOrder.ts#L63)）优化目标是“形状变化相似性 + 厚度/长度权重 + 层次聚类 + 局部交换”。  
PID 排序优化目标是“概率包含深度（中心性）”。两者目标函数不同，所以效果会有明显取舍。

当前实现里，PID 页面对比用的 Sine 参考排序是“全时间窗、ROI 无关”的固定参考（更公平），见 [src/App.vue](e:/Project/2026Journal/Code/src/App.vue#L104) 和 [src/App.vue](e:/Project/2026Journal/Code/src/App.vue#L816)。

你问的指标变化（wiggle / sine illusion）要分两层看：

1. `PID + basic(center baseline)`  
- 往往更强调“语义中心层”聚拢，不直接优化曲率。  
- 相比 Sine 排序，`wiggle` 和 `sine-illusion proxy`（二阶差分相关）通常更容易上升或波动。  
指标定义见 [src/layout/metrics.ts](e:/Project/2026Journal/Code/src/layout/metrics.ts#L35)、[src/layout/metrics.ts](e:/Project/2026Journal/Code/src/layout/metrics.ts#L94)、[src/layout/metrics.ts](e:/Project/2026Journal/Code/src/layout/metrics.ts#L108)。

2. `PID + L2 wiggle baseline`  
- baseline 会主动压摆动，所以 `wiggle` 通常比 basic PID 明显下降。  
- 但因为层顺序仍是 PID，`sine illusion` 未必总能和 Sine 排序一样低，取决于排名改动幅度（slopegraph 里能看到）。

补充一个真实现状：  
PID tab 目前在 Metrics 表里是 `N/A`（未像 optimize/braided 那样直接算 before/after 指标表），见 [src/App.vue](e:/Project/2026Journal/Code/src/App.vue#L168)。  
它现在给的是排序变化摘要（changed ratio、avg rank shift 等），见 [src/App.vue](e:/Project/2026Journal/Code/src/App.vue#L261)。

---

**3. 结合当前 covid case，PID 排序在语义解读上的增益**

当前 covid 里你实际在看的是 `state|h1` 这一组层（代码会把 horizon 固定到 h1），见 [src/App.vue](e:/Project/2026Journal/Code/src/App.vue#L483)。

用 PID 排序的语义增益主要有三点：

1. 更容易看出“共识层 vs 离群层”  
- PID 高的层：其中位轨迹经常被其他州的不确定区间覆盖，代表“群体共识/典型走势”。  
- PID 低的层：常在群体区间外，代表“离群州/异常阶段”。

2. 更利于读“跨州不确定性重叠关系”  
- Sine 排序更像几何光滑优先。  
- PID 排序更像统计中心性优先。  
在政策/流行病学语境里，后者更接近“哪些州行为更代表整体”。

3. 对“解释型分析”更友好  
- 你在汇报时可以直接说：  
“这个州排名上升，不是因为曲线更平滑，而是它在概率意义上更像‘被多数州区间覆盖的中心行为’。”  
这比纯视觉平滑优化更有语义支撑。

如果你愿意，我下一步可以把 PID tab 也接入和 optimize 一样的定量 metrics 表（直接输出 `wiggle/illusion` 的 before/after 数值），这样你就能在同一个面板里看到严格数值对比。